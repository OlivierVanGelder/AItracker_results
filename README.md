# SE Ranking export bot

## Setup
1. Install dependencies
   npm install

2. Install Playwright browser
   npm run pw:install

3. Create env file
   cp .env.example .env
   Fill in SE_RANKING_GUEST_URL and WEBHOOK_URL

## Run locally
npm start
